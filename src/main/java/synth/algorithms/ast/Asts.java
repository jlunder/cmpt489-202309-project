package synth.algorithms.ast;

public class Asts {
    ExprNode optimizeExpr(ExprNode node) {
        // fold constants
        // canonicalize
        return node;
    }

    BoolNode optimizeBool(BoolNode node) {
        // fold constants
        // canonicalize
        return node;
    }
}
